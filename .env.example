# Consumed by docker-compose.yml as the `API_SERVER` build arg for the react
# service, which bakes it in as REACT_APP_API_SERVER at build time.
# Must be the URL the BROWSER uses to reach the backend, not an internal
# container hostname.
ENV_API_SERVER=http://127.0.0.1
