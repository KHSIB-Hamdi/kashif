# Postgres container credentials (docker-compose.yml -> db service).
# Must match DB_USER / DB_PASSWORD / DB_DATABASE in backend/.env.
POSTGRES_USER=replace-me
POSTGRES_PASSWORD=replace-me
POSTGRES_DB=predictiondb
